{
  "name": "@app/integration-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "shx rm -rf build",
    "lint": "eslint --fix src --ext .ts --quiet --no-error-on-unmatched-pattern",
    "analyze": "eslint src --ext .ts --quiet --no-error-on-unmatched-pattern",
    "build": "npm -v",
    "test": "jest",
    "deploy": "npm -v"
  },
  "devDependencies": {
    "@app/backend": "file:../backend",
    "@aws-sdk/client-dynamodb": "^3.215.0",
    "@aws-sdk/client-lambda": "^3.388.0",
    "@types/jest": "27.0.2",
    "jest": "27.3.1",
    "npm-run-all": "4.1.5",
    "typescript": "4.9.4"
  },
  "dependencies": {}
}
